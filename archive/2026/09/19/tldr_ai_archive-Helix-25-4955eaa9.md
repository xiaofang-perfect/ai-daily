---
title: "Helix 2.5"
source: TLDR AI · 2026-09-18
url: https://x.com/figure_robot/status/2100657350952779925?utm_source=tldrai
date: 2026-09-19
published_at: 2026-09-18T12:00:00+00:00
tag: 产品发布
item_id: 4955eaa9ebce0f6a
---
![@Figure_robot](https://pbs.twimg.com/profile_images/1976273310431444992/k9xGabdN_normal.jpg)

Today we’re releasing Helix 2.5
We rented 30 homes in the Bay Area. The robots arrived with no additional training and started doing useful work

00:00

[18:45 · 2026年9月17日](https://x.com/Figure_robot/status/2100657350952779925)


317.5万

Views

- ![@Figure_robot](https://pbs.twimg.com/profile_images/1976273310431444992/k9xGabdN_normal.jpg) Today we’re releasing Helix 2.5 We rented 30 homes in the Bay Area. The robots arrived with no additional training and started doing useful work ![](https://pbs.twimg.com/amplify_video_thumb/2100653191511891968/img/6uzlRqKg6QU2AD3y?format=webp&name=medium) 00:00 [18:45 · 2026年9月17日](https://x.com/Figure_robot/status/2100657350952779925)(function (id, args) { var element = document.getElementById(id); if (!element) return; element.textContent = (function formatFullTimestamp({ timestamp, lang, timeZone }) { const date = new Date(timestamp); return `${date.toLocaleTimeString(lang, { hour: "numeric", minute: "2-digit", timeZone })} · ${date.toLocaleDateString(lang, { month: "short", day: "numeric", year: "numeric", timeZone })}`; })(args); })("_R_bqpr5ipip8j6_", {"timestamp":1789670715000,"lang":"zh-cn","nowLabel":"Now"}); - ![@Figure_robot](https://pbs.twimg.com/profile_images/1976273310431444992/k9xGabdN_normal.jpg) [9月17日](https://x.com/Figure_robot/status/2100657353679122780)(function (id, args) { var element = document.getElementById(id); if (!element) return; element.textContent = (function formatRelativePastTimestamp({ timestamp, lang, timeZone, nowLabel }) { const date = new Date(timestamp); const now = /* @__PURE__ */ new Date(); const diffInSeconds = Math.floor((now.getTime() - date.getTime()) / 1e3); if (diffInSeconds <= 0) return nowLabel ?? new Intl.RelativeTimeFormat(lang, { numeric: "auto" }).format(0, "second"); if (diffInSeconds < 60) return new Intl.NumberFormat(lang, { style: "unit", unit: "second", unitDisplay: "narrow" }).format(diffInSeconds); if (diffInSeconds < 3600) return new Intl.NumberFormat(lang, { style: "unit", unit: "minute", unitDisplay: "narrow" }).format(Math.floor(diffInSeconds / 60)); if (diffInSeconds < 3600 * 24) return new Intl.NumberFormat(lang, { style: "unit", unit: "hour", unitDisplay: "narrow" }).format(Math.floor(diffInSeconds / 3600)); if (date.toLocaleDateString(lang, { year: "numeric", timeZone }) === now.toLocaleDateString(lang, { year: "numeric", timeZone })) return date.toLocaleDateString(lang, { month: "short", day: "numeric", timeZone }); return date.toLocaleDateString(lang, { month: "short", day: "numeric", year: "numeric", timeZone }); })(args); })("_R_bslbdl5ipip8j6_", {"timestamp":1789670716000,"lang":"zh-cn","nowLabel":"Now"}); Here's 4 hours of our robot doing zero-shot work in 30 rental homes![](https://pbs.twimg.com/amplify_video_thumb/2100656010772062208/img/MTB68oKnnEVCre8t?format=webp&name=medium) 00:00 ![@Figure_robot](https://pbs.twimg.com/profile_images/1976273310431444992/k9xGabdN_normal.jpg) [9月17日](https://x.com/Figure_robot/status/2100657690758480223)(function (id, args) { var element = document.getElementById(id); if (!element) return; element.textContent = (function formatRelativePastTimestamp({ timestamp, lang, timeZone, nowLabel }) { const date = new Date(timestamp); const now = /* @__PURE__ */ new Date(); const diffInSeconds = Math.floor((now.getTime() - date.getTime()) / 1e3); if (diffInSeconds <= 0) return nowLabel ?? new Intl.RelativeTimeFormat(lang, { numeric: "auto" }).format(0, "second"); if (diffInSeconds < 60) return new Intl.NumberFormat(lang, { style: "unit", unit: "second", unitDisplay: "narrow" }).format(diffInSeconds); if (diffInSeconds < 3600) return new Intl.NumberFormat(lang, { style: "unit", unit: "minute", unitDisplay: "narrow" }).format(Math.floor(diffInSeconds / 60)); if (diffInSeconds < 3600 * 24) return new Intl.NumberFormat(lang, { style: "unit", unit: "hour", unitDisplay: "narrow" }).format(Math.floor(diffInSeconds / 3600)); if (date.toLocaleDateString(lang, { year: "numeric", timeZone }) === now.toLocaleDateString(lang, { year: "numeric", timeZone })) return date.toLocaleDateString(lang, { month: "short", day: "numeric", timeZone }); return date.toLocaleDateString(lang, { month: "short", day: "numeric", year: "numeric", timeZone }); })(args); })("_R_bslbel5ipip8j6_", {"timestamp":1789670796000,"lang":"zh-cn","nowLabel":"Now"}); Helix 2.5 was built to answer a harder question: can a humanoid enter a home it has never seen and immediately get to work, with its whole body, on its own? We posted a detailed write-up:![](https://pbs.twimg.com/card_img/2100656834218774528/ZYHnDTbj?format=webp&name=medium)
- ![@yishan](https://pbs.twimg.com/profile_images/887132881289662464/7sz43Ijt_normal.jpg) [9月17日](https://x.com/yishan/status/2100697041487962301)(function (id, args) { var element = document.getElementById(id); if (!element) return; element.textContent = (function formatRelativePastTimestamp({ timestamp, lang, timeZone, nowLabel }) { const date = new Date(timestamp); const now = /* @__PURE__ */ new Date(); const diffInSeconds = Math.floor((now.getTime() - date.getTime()) / 1e3); if (diffInSeconds <= 0) return nowLabel ?? new Intl.RelativeTimeFormat(lang, { numeric: "auto" }).format(0, "second"); if (diffInSeconds < 60) return new Intl.NumberFormat(lang, { style: "unit", unit: "second", unitDisplay: "narrow" }).format(diffInSeconds); if (diffInSeconds < 3600) return new Intl.NumberFormat(lang, { style: "unit", unit: "minute", unitDisplay: "narrow" }).format(Math.floor(diffInSeconds / 60)); if (diffInSeconds < 3600 * 24) return new Intl.NumberFormat(lang, { style: "unit", unit: "hour", unitDisplay: "narrow" }).format(Math.floor(diffInSeconds / 3600)); if (date.toLocaleDateString(lang, { year: "numeric", timeZone }) === now.toLocaleDateString(lang, { year: "numeric", timeZone })) return date.toLocaleDateString(lang, { month: "short", day: "numeric", timeZone }); return date.toLocaleDateString(lang, { month: "short", day: "numeric", year: "numeric", timeZone }); })(args); })("_R_5ualnn5ipip8j6_", {"timestamp":1789680178000,"lang":"zh-cn","nowLabel":"Now"}); “You just saw Helix do three tasks in this home” > camera cuts away from each task before any of them successfully finishes Look, I’m not just a hater, I want you guys to succeed, but this was an anti-convincing video.
